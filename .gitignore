build
.coverage
docs/_build/
docs/locale/**/*.mo
demo*/
*.egg-info
__pycache__/
*.pyc
.env*
.wizolt
.minacode
.nanocode
.codegraph/
.venv/
.mypy_cache/
.ruff_cache/
.python-version
.code-symbol-index/
.code-workflow-probe.json

# pasted screenshots dropped at the repo root; snapshots/ stays tracked
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp
/*.bmp
/*.tiff
/*.heic
/*.avif

# crash core dumps
core
core.*
.playwright-cli/
.DS_Store

# Working notes for the async runtime migration: the plan and the user-approved deviations from
# it. Both describe how the work is being done, not what the code is; they stay out of history.
ASYNC_RUNTIME_MIGRATION_SPEC.md
ASYNC_MIGRATION_DEVIATIONS.md
